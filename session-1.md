# آزمایشگاه سیستم‌عامل - تمرینات عملی (Hands-on Labs)
## جلسات ۱ و ۲: آشنایی با لینوکس و مدیریت فایل

این مستند شامل تمرینات عملی و دستورالعمل‌های مربوط به جلسات اول و دوم آزمایشگاه سیستم‌عامل می‌باشد. دانشجویان موظف‌اند این فایل را تکمیل کرده و دستورات استفاده شده را در بخش‌های مشخص شده وارد کنند.

---

### بخش اول: آماده‌سازی محیط لینوکس (جلسه ۱)

**هدف:** آشنایی با فرآیند نصب توزیع‌های لینوکس در ماشین مجازی و درک مفاهیم اولیه.

#### تمرین ۱: نصب سیستم‌عامل Ubuntu
مراحل زیر را در نرم‌افزار مجازی‌ساز خود (مانند Oracle VM VirtualBox) انجام دهید:  
۱.یک ماشین مجازی جدید ایجاد کنید و نام آن را به فرمت `[شماره دانشجویی]-[نام خانوادگی]` قرار دهید.  
۲. منابع سیستم را تنظیم کنید (حداقل ۵۱۲ مگابایت رم، پیشنهاد ما ۲ گیگابایت است).  
۳. یک هارد دیسک مجازی از نوع `VDI` با تخصیص پویای فضا (`Dynamically Allocated`) و حجم حداقل ۱۵ گیگابایت بسازید.  
۴. فایل ISO توزیع اوبونتو را آپلود کرده و فرآیند نصب (`Erase Disk and Install Ubuntu`) را تکمیل کنید.  
۵. **بسیار مهم:** پس از راه‌اندازی موفق سیستم، حتماً از ماشین مجازی خود یک **Snapshot** تهیه کنید.

> **تأییدیه بخش اول:**
> لطفاً اسکرین‌شاتی از صفحه دسکتاپ لینوکس خود که در آن ترمینال باز است را به همراه این فایل تحویل دهید.

---

### بخش دوم: مدیریت فایل‌ها، جستجو و مجوزها (جلسه ۲)

**هدف:** تسلط بر خط فرمان لینوکس (CLI)، مدیریت فایل‌ها، جستجوی پیشرفته و مفاهیم مالکیت و سطوح دسترسی (Permissions).

ترمینال لینوکس را با کلیدهای میانبر `Ctrl+Alt+T` باز کرده و تسک‌های زیر را انجام دهید. دستورات خود را زیر هر سوال بنویسید.

#### تمرین ۲: مدیریت دایرکتوری و مجوزها (Permissions)
یک دایرکتوری جدید در میزکار (Desktop) خود بسازید. سپس چند فایل نمونه در آن ایجاد کنید. با استفاده از دستورات لینوکس، **دسترسی نوشتن (Write) و اجرا/جستجو (Execute)** را از **سایر افراد (Others)** بگیرید.
```bash
# دستورات خود را اینجا وارد کنید
```
#### تمرین ۳: گروه‌ها و تغییر مالکیت (Ownership)

ابتدا دستور مناسب برای نمایش لیستی از تمام گروه‌هایی که نام کاربری شما در آن‌ها عضو است را بنویسید. سپس مالکیت گروهی (Group Ownership) دایرکتوری که در تمرین قبل ساختید را به یکی دیگر از گروه‌های خود (به جز گروه اصلی) تغییر دهید.
```bash
# دستورات خود را اینجا وارد کنید

```
#### تمرین ۴: کار با Timestamp فایل‌ها

یک فایل جدید ایجاد کنید. با استفاده از تنها یک خط دستور، زمان دستیابی (Access Time) این فایل را به ساعت ۷ صبح روز گذشته تغییر دهید. سپس با استفاده از دستوری دیگر، زمان‌های تغییر یافته‌ی فایل را نمایش دهید.
```bash
# دستورات خود را اینجا وارد کنید
```
#### تمرین ۵: جستجوی پیشرفته و اجرای مستقیم (Find & Exec)

با استفاده از دستور find، در کل دایرکتوری‌های موجود، فایل‌های کاملاً خالی را پیدا کنید و در همان یک خط دستور، بلافاصله آن‌ها را پاک کنید.
```bash
# دستورات خود را اینجا وارد کنید
```

#### تمرین ۶: تحلیل مجوزهای خاص

دستور زیر دقیقاً چه کاری روی file.txt انجام می‌دهد؟ مفهوم عدد 4664 را با توجه به ساختار مجوزهای دسترسی (User, Group, Other) و مفاهیم پایه توضیح دهید.
```Bash

chmod 4664 file.txt
```

#### پاسخ تحلیلی شما:

    (تحلیل خود را در این قسمت یادداشت کنید. مشخص کنید که هر رقم به چه معناست و چه دسترسی‌هایی به فایل داده می‌شود).
